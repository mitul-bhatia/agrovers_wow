# Requirements for preprocessing ONLY (run locally, not on Render)
# Install with: pip install -r requirements-preprocessing.txt

sentence-transformers>=2.7.0
torch>=2.0.0
transformers>=4.30.0
huggingface-hub>=0.20.0
